# Frontend-only dependencies (used by Streamlit Cloud)
# The backend (FastAPI, DB, LLM) is deployed separately on Render.
# Streamlit Cloud reads this file — it must NOT contain psycopg2, sqlalchemy, etc.

streamlit==1.41.1
requests==2.32.3
python-dotenv==1.0.1
